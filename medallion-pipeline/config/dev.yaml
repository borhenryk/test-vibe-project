# Development Environment Configuration
# Used for local development and testing

environment: dev
catalog: dbdemos_henryk
schema: medallion_pipeline

# Compute settings
compute:
  # Use serverless by default for dev
  use_serverless: true

# Data settings
data:
  source: sklearn_iris
  version: "1.0"

# Pipeline settings
pipeline:
  schedule_enabled: false
  notification_emails: []
